spring:
  application:
    name: code-generator
  cloud:
    nacos:
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
      config:
        shared-configs[0]:
          data-id: code-generator.yml
          refresh: true