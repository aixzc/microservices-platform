<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.central.ocr.mapper.AnalysisFileMapper">
    <select id="findList" resultType="com.central.ocr.model.AnalysisFile">
        select t.*, f.name fileName, f.path filePath from ocr_analysis_file t
        left join file_center.file_info f on t.analysis_file_id = f.id
    </select>
</mapper>