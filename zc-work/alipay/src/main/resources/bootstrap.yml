spring:
  application:
    name: alipay
  cloud:
    nacos:
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
      config:
        shared-configs[0]:
          data-id: alipay.yml
          refresh: true